
import React from 'react'
import { SafeAreaView, ScrollView } from 'react-native'

export const Schedule = ({navigation, route}) => {

  return (
    <SafeAreaView>
        <ScrollView>
            <Text> Scheduling something</Text>
        </ScrollView>
    </SafeAreaView>
  )

}
